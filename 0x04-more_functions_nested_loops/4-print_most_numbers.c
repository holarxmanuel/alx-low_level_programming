#include "main.h"

/**
 * print_most_numbers - this function prints njmbers except 2 4
 * Return: 0
 */
void print_most_numbers(void)
{
	int lent = 0;

	while (s[i] != '\0')
		i++;
	for (i = i - 1; i >= 0; i--)
		_putchar(s[i]);
	_putchar('\n');
}
