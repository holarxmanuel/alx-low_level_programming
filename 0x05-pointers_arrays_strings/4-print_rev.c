#include "main.h"

/**
 * print_rev - this prints the string in reverse
 * @s: the input
 * Return: reversd string
 */
void print_rev(char *s)
{
	int i;

	while (i != '\0')
		i++;
	while (i >= 0)
		_putchar(str[i]);
	_putchar('\n');
}
